# docker-compose.yml
version: "3.8"

services:
    app:
        build: .
        ports:
            - "3000:3000"
        environment:
            - NODE_ENV=development
            - JWT_SECRET=U9x$Pz@7#kVw!zE4mNq^BfJrT1LsGxYd
            - DATABASE_URL=postgresql://postgres:FDCmOTzNrhOCmlQgFjksweRLsluBGLre@ballast.proxy.rlwy.net:50676/railway
        depends_on:
            - db
        networks:
            - task-network

    db:
        image: postgres:15-alpine
        environment:
            - POSTGRES_USER=postgres
            - POSTGRES_PASSWORD=FDCmOTzNrhOCmlQgFjksweRLsluBGLre
            - POSTGRES_DB=railway
        ports:
            - "50676:50676"
        volumes:
            - postgres-data:/var/lib/postgresql/data
        networks:
            - task-network

volumes:
    postgres-data:

networks:
    task-network:
